#!/usr/bin/env bash
# display the content for current dir. in list format

for file in *; do
    if [[ ! $file =~ ^\. && $file =~ - ]]; then

	name-part=$(echo "$file" | cut -d'-' -f2-)

	echo "- $name_part"

    fi

done
